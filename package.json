{
  "type": "module",
  "name": "fun-with-web-components",
  "devDependencies": {
    "@babel/core": "^8.0.1",
    "@babel/preset-env": "^8.0.2",
    "@babel/preset-typescript": "^8.0.1",
    "@testing-library/dom": "^10.4.1",
    "@types/jest": "^30.0.0",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "build:src": "tsc --project tsconfig.json",
    "build:test": "tsc --project tsconfig.test.json",
    "clean:src": "rm -rf build",
    "clean:test": "rm -rf build-test",
    "test": "jest"
  },
  "dependencies": {
    "dompurify": "^3.4.12"
  }
}
